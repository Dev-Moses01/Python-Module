print(bin(50))
print(bin(70))

print(bin(50 & 70))
#AND (&) sets each bit to 1 if both are 1
#OR (|) sets each bit to 1 if atleast one of the bits is one
#XOR (^) sets the bit to 1 if the bits are different
#NOT (~) bitwise negation == 1 becomes 0 and vice versa

print(int("1111", 2))
